{
  "name": "cinema-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Cinema booking system monorepo",
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build:api": "pnpm --filter api build",
    "build:web": "pnpm --filter web build",
    "lint:api": "pnpm --filter api lint",
    "lint:web": "pnpm --filter web lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.2.2",
      "onFail": "download"
    }
  }
}
